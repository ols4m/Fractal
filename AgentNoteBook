# Fractal — Agent Notebook

## Original LLM + Backend Strategy
*(See original notes below for FastAPI + structured JSON output approach)*

---

## Frontend: Wikipedia Map (controversial/wikipedia-map)

**Repo:** https://github.com/controversial/wikipedia-map
**License:** Open source — fork freely

---

### What It Is

A web app that visualizes connections between topics as an interactive node graph. The user enters a topic, a node appears, they click to expand it, and new connected nodes branch out. This is essentially the same interaction model Fractal needs — it's a near-perfect base to build on.

---

### Tech Stack (What's Inside)

**Graph Rendering:**
- `vis.js` (vis-network) — the core graph library that draws and animates all nodes and edges
- Hosted via CDN: `https://cdnjs.cloudflare.com/ajax/libs/vis/4.16.1/vis-network.min.js`

**UI Libraries:**
- `shepherd.js` — guided tour/onboarding for users
- `ionicons` — icon set for buttons (GitHub, help, shuffle, trash icons)
- `clipboard.js` — copy-to-clipboard functionality

**Frontend Files:**
```
index.html              ← main entry point, loads everything
css/
  style.css             ← main layout styles
  bar.css               ← top search bar styles
  buttongroup.css       ← bottom-right button group
  shepherd-custom.css   ← custom tour styles
  commafield.css        ← multi-topic input field
  welcome.css           ← welcome/info box
  modals.css            ← modal dialogs
  progress.css          ← loading progress bars
js/
  wikipedia_parse.js    ← fetches + parses Wikipedia API (REPLACE THIS for Fractal)
  helpers.js            ← utility functions
  main_functions.js     ← core graph logic (ADD NODES, EXPAND, COLOR)
  main.js               ← main app logic
  help.js               ← tutorial/tour logic
  bindings.js           ← event listeners (clicks, keypresses)
  commafield.js         ← multi-input tag field
  network_serialize.js  ← save/share graph state via URL
  loadsaved.js          ← load shared graph from URL
  modals.js             ← modal open/close logic
  progress.js           ← loading bar logic
```

---

### How It Works (Original Flow)

```
User types topic → clicks Go
→ wikipedia_parse.js calls Wikipedia API
→ gets linked articles from first paragraph
→ main_functions.js adds them as nodes to vis.js graph
→ nodes colored by distance from center node
→ user clicks a node → expands further
```

---

### How To Adapt It For Fractal

The key insight: **only one file needs to be replaced** to swap Wikipedia data for Fractal/LLM data.

**Replace `wikipedia_parse.js` with `fractal_api.js`**

Instead of calling the Wikipedia API, call your Groq/Ollama backend:

```javascript
// fractal_api.js — replaces wikipedia_parse.js

async function getSubPages(query) {
  const response = await fetch('http://localhost:8000/generate-map', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ query: query })
  });
  const data = await response.json();

  // Return node names so main_functions.js can add them to the graph
  return data.nodes.map(node => node.name);
}
```

Everything else — the graph rendering, the click-to-expand, the animations, the UI — stays exactly the same.

---

### What To Customize For Fractal Branding

| File | What To Change |
|---|---|
| `index.html` | Title, meta description, welcome message text |
| `css/style.css` | Background color, font, overall aesthetic |
| `css/bar.css` | Search bar appearance, placeholder text |
| `css/welcome.css` | Landing message ("Wikipedia Map" → "Fractal") |
| `js/wikipedia_parse.js` | Replace entirely with `fractal_api.js` |
| `js/help.js` | Update tutorial text to describe Fractal, not Wikipedia |
| `js/main_functions.js` | Node colors — map domain colors to Fractal's color scheme |

**Node color logic** lives in `main_functions.js`. Currently colors nodes by distance from center. For Fractal you'll want to color by:
- Domain nodes → their assigned color (blue, green, etc.)
- Emergent/grey node → grey
- Tension edges → styled differently than regular connections

---

### What To Keep Exactly As-Is

- All of `vis.js` graph rendering
- `bindings.js` — click/keypress handling works perfectly
- `network_serialize.js` — share-by-URL feature is a great free feature to keep
- `commafield.js` — multi-topic input is useful for Fractal too
- `modals.js` and `progress.js` — loading states and modals are needed

---

### Step-by-Step Integration Plan

1. **Fork the repo** into your own GitHub
2. **Clone it locally** — open `index.html` in browser to confirm it works with Wikipedia first
3. **Create `js/fractal_api.js`** — replaces `wikipedia_parse.js`, points to your backend
4. **Update `index.html`** — swap `wikipedia_parse.js` script tag for `fractal_api.js`
5. **Restyle** — update CSS files and welcome text for Fractal branding
6. **Update node colors** in `main_functions.js` to match Fractal's color scheme
7. **Test locally** with Ollama backend running
8. **Deploy** pointing to Groq backend for the live demo

---

### Important Note on Licensing

This repo is open source. You can fork and modify it freely. You should:
- Keep a note in your code crediting the original (`vis.js` graph rendering based on wikipedia-map by Luke Deen Taylor)
- Remove all Wikipedia-specific branding from the UI
- The core logic is yours to adapt

---

## Original LLM Backend Strategy (FastAPI + Groq/Ollama)

*(Original AgentNotebook content preserved below)*

---

### Goal
Use an LLM to generate structured domain nodes, suggest tensions, and create 1 emergent grey synthesis. You control the structure.

### LLM Prompt Schema
```json
{
  "domains": [{"name": "", "summary": ""}],
  "tensions": [{"source": "", "target": "", "relationship": ""}],
  "emergent_pattern": {"label": "", "summary": ""}
}
```

### FastAPI Endpoint (swap OpenAI for Groq)
```python
from fastapi import FastAPI
from pydantic import BaseModel
import httpx
import os
import json

GROQ_API_KEY = os.getenv("GROQ_API_KEY")
GROQ_API_URL = "https://api.groq.com/openai/v1/chat/completions"
MODEL = "llama3-8b-8192"
TEMPERATURE = 0.3

# For local dev with Ollama, swap these:
# GROQ_API_URL = "http://localhost:11434/api/chat"
# MODEL = "llama3"

DEMO_MODE = {
    "nodes": [
        {"name": "Demo Domain A", "summary": "Example summary A", "color": "blue"},
        {"name": "Demo Domain B", "summary": "Example summary B", "color": "green"},
        {"name": "Emergent Pattern", "summary": "Grey synthesis demo", "color": "grey"},
    ],
    "edges": [
        {"source": "Demo Domain A", "target": "Demo Domain B", "relationship": "tension"}
    ]
}

app = FastAPI()

class MapRequest(BaseModel):
    query: str

def validate_llm_output(output_json):
    try:
        domains = output_json.get("domains", [])
        emergent = output_json.get("emergent_pattern", {})
        if not (3 <= len(domains) <= 5): return False
        if not emergent.get("summary"): return False
        for d in domains:
            if not d.get("summary") or not d.get("name"): return False
        return True
    except Exception:
        return False

def transform_to_graph(output_json):
    nodes = []
    edges = []
    for d in output_json["domains"]:
        nodes.append({"name": d["name"], "summary": d["summary"], "color": "blue"})
    ep = output_json["emergent_pattern"]
    nodes.append({"name": ep["label"], "summary": ep["summary"], "color": "grey"})
    for t in output_json.get("tensions", []):
        edges.append({"source": t["source"], "target": t["target"], "relationship": t["relationship"]})
    return {"nodes": nodes, "edges": edges}

@app.post("/generate-map")
async def generate_map(request: MapRequest):
    try:
        prompt = f"""
You are generating a structured research field map for Fractal.
Return JSON only. No prose. No explanations.

{{
  "domains": [{{"name": "", "summary": ""}}],
  "tensions": [{{"source": "", "target": "", "relationship": ""}}],
  "emergent_pattern": {{"label": "", "summary": ""}}
}}

Rules:
- 3-5 domains only
- At least 1 emergent pattern
- No empty summaries
- Avoid buzzwords
- Focus on structural forces and real tensions
- JSON only

Query: {request.query}
"""
        async with httpx.AsyncClient() as client:
            response = await client.post(
                GROQ_API_URL,
                headers={"Authorization": f"Bearer {GROQ_API_KEY}"},
                json={
                    "model": MODEL,
                    "messages": [{"role": "user", "content": prompt}],
                    "temperature": TEMPERATURE
                }
            )
        content = response.json()["choices"][0]["message"]["content"]
        output_json = json.loads(content)
        if not validate_llm_output(output_json):
            return DEMO_MODE
        return transform_to_graph(output_json)
    except Exception as e:
        print("Error:", e)
        return DEMO_MODE
```

### Dev vs Prod Config

| Setting | Ollama (Dev) | Groq (Prod) |
|---|---|---|
| API URL | http://localhost:11434/api/chat | https://api.groq.com/openai/v1/chat/completions |
| API Key | none | GROQ_API_KEY env var |
| Model | llama3 | llama3-8b-8192 |

Change only these 3 lines to switch between environments.
